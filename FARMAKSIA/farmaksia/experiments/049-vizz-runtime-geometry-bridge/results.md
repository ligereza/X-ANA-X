# Resultados — Experimento 049

Estado observado: `VIZZ_049_RUNTIME_BRIDGE_CONTRACT_VALID`.

Un `GazeSample` con sólo features legacy y ángulos binoculares queda en
`UNKNOWN: missing_world_geometry`. El mismo sample con centros oculares,
dirección mundial y layout sintético resuelve el monitor primario.

Esto prueba el cierre de seguridad del puente, no la disponibilidad de una
geometría mundial real en la cámara actual.
